//id = edad




function mostrar()//(if (parseInt(edad.value)=15;alert("niña bonita")) {})
{
//tomo la edad  
	//var age = parseInt(edad.value)
	var age;

	age = edad.value;

		if (age == 15) { // Comparacion es con 2 = [==] / 1 solo = es para asignar!!!
				alert("Niña bonita");
		}
		//else	alert("no bonita");

}//FIN DE LA FUNCIÓN