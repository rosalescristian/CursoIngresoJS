function mostrar()
{
//tomo la edad  
	var age = edad.value

		if (age >= 18) { // Comparacion es con 2 = [==] / 1 solo = es para asignar!!!
				alert("Ud. es mayor de edad.");
		}


}//FIN DE LA FUNCIÓN