function mostrar()
{
//tomo la edad  

	var age;
	age = edad.value;
	age = parseInt(age);

		if (age > 12 && age < 18)
		{
				alert("Ud. es un adolescente.");
		}

/*
		if (age>12)
		{
			if (age<18)
			{
				alert("Ud. es un adolescente.");
			}
					
		}
*/
}//FIN DE LA FUNCIÓN